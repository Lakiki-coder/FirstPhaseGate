def differenc_in_age(current_sun_age, current_father_age):

	if (current_son_age + current_son_age > current_father_age):

		raise ValueError("invalid input")

		difference_in_year =  current_father_age - (current_son_age + current_son_age)

	if(current_father_age == 0 or current_son_age == 0) :

		raise ValueError("invalid input")

	if(type(current_father_age == str) or type(current_son_age == str)):

		raise ValueError("invalid input")

	if(type(current_father_age == float) or type(current_son_age == float):

		raise ValueError("invalid input")

	if(currentFatherAge < currentSonAge):
		
		raise ValueError("invalid input")

	else:
		return difference_in_year
